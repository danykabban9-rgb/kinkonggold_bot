services:
  - type: worker
    name: kinkonggold_bot
    env: python
    buildCommand: |
      apt-get update && apt-get install -y build-essential libatlas-base-dev
      pip install -r requirements.txt
    startCommand: python bot.py
